# Implement a Markdown Table Parser

Implement `MarkdownTableParser.Parse(string markdown)` in the starter project.

The parser must support header rows, separator rows, trimmed cells, escaped pipes, malformed input errors, and deterministic row dictionaries keyed by header name.
