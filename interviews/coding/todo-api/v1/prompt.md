# Implement a Todo API

Implement the in-memory ASP.NET Core Todo API described in the starter README.

The API must expose deterministic JSON, stable status codes, validation for empty titles, monotonic integer IDs, and update/delete behavior for missing items.
