# Write Tests for Order Discount Rules

Production code already exists. Add deterministic unit tests that cover discount thresholds, loyalty discount behavior, invalid totals, and rounding behavior.

Do not change production behavior unless you find a compile error.
