# Write Tests for a Retry Policy

Production code already exists. Add tests for retry success, exhausted retries, non-retryable exceptions, delay calculation, and cancellation.

Tests must be deterministic and must not sleep in real time.
